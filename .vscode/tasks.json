{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "build",
      "group": "build",
      "problemMatcher": [],
      "label": "npm: build",
      "detail": "pnpm clean && tsc --project tsconfig.build.json && tsc-alias -p tsconfig.build.json"
    }
  ]
}
